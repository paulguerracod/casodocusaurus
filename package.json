{
  "name": "my-website",
  "version": "0.0.0",
  "private": true,
  
  "homepage": "https://paulguerracod.github.io/docusaurus",
    "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "predeploy": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "build:gh-pages": "DEPLOY_TARGET=gh-pages npm run build",
    "build:cpanel": "DEPLOY_TARGET=cpanel npm run build",
    "deploy:gh-pages": "npm run build:gh-pages && gh-pages -d build -b gh-pages",
    "deploy:cpanel": "npm run build:cpanel && echo 'Sube manualmente la carpeta build/ a cPanel'"
  },
  "dependencies": {
    "@docusaurus/core": "3.7.0",
    "@docusaurus/preset-classic": "3.7.0",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.7.0",
    "@docusaurus/types": "3.7.0",
    "gh-pages": "^6.1.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}